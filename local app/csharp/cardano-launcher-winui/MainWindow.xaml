<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CardanoLauncher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Cardano Community Suite"
    Width="700"
    Height="550"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="20" Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Spacing="8" HorizontalAlignment="Center">
            <TextBlock
                Text="🔗 Cardano Community Suite"
                FontSize="36"
                FontWeight="Bold"
                TextAlignment="Center"
                Foreground="#670883" />
            <TextBlock
                Text="Integrated Tools for End-Users & Administrators"
                FontSize="14"
                TextAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Main Content -->
        <StackPanel Grid.Row="1" Spacing="16">

            <!-- End-User Tools Button -->
            <Button
                x:Name="BtnEndUser"
                Click="BtnEndUser_Click"
                Height="80"
                HorizontalAlignment="Stretch">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        Text="👤"
                        FontSize="36"
                        VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                        <TextBlock
                            Text="End-User Tools"
                            FontSize="16"
                            FontWeight="SemiBold" />
                        <TextBlock
                            Text="Generate keypairs • Sign messages • Export wallets • Verify signatures"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </Button>

            <!-- Admin Dashboard Button -->
            <Button
                x:Name="BtnAdmin"
                Click="BtnAdmin_Click"
                Height="80"
                HorizontalAlignment="Stretch">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        Text="👨‍💼"
                        FontSize="36"
                        VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                        <TextBlock
                            Text="Admin Dashboard"
                            FontSize="16"
                            FontWeight="SemiBold" />
                        <TextBlock
                            Text="Generate challenges • Verify signatures • Check on-chain stakes • Manage registry"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </Button>

            <!-- Documentation Button -->
            <Button
                x:Name="BtnDocs"
                Click="BtnDocs_Click"
                Height="80"
                HorizontalAlignment="Stretch">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        Text="📖"
                        FontSize="36"
                        VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                        <TextBlock
                            Text="Documentation & Resources"
                            FontSize="16"
                            FontWeight="SemiBold" />
                        <TextBlock
                            Text="Architecture • User flows • API specifications • Security models"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </Button>

        </StackPanel>

        <!-- Footer Section -->
        <Grid Grid.Row="3" VerticalAlignment="Bottom">
            <TextBlock
                Text="Cardano Community Suite v1.0 • Secure. Open. Community-Driven."
                FontSize="12"
                TextAlignment="Center"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
        </Grid>

    </Grid>

</Window>
